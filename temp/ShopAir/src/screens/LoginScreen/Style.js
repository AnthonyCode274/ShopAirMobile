import { Colors, Sizes } from '@assets';
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        
    },
    safeAreaView: {},
    logo: {
        width: Sizes.width / 2,
        maxHeight: Sizes.height / 6
    }
});
